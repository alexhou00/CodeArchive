#include <iostream>
using namespace std;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	string girl, boy;
	getline(cin, girl);
	getline(cin, boy);
	cout << girl << " and " << boy << " sitting in the tree ";
	return 0;
}
