// what's wrong with the author of this problem...
// don't be tricked...
#include <iostream>
using namespace std;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	int n;
	cin >> n;
	cout << "2^(10^" << n << ")";
	return 0;
}
